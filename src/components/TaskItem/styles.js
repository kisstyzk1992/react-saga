const styles = () => ({
    CardActions: {
        display: "felx",
        alignItems: "center",
        justifyContent: "flex-end"
    },

    card: {
        marginTop: 10
    }
})
export default styles